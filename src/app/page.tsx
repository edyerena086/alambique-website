// Import core libraries
import React, { FC } from 'react'

// Import components
import { SectionTitle } from '@app/components'

const Home: FC = () => {
  return (
    <SectionTitle>
      Welcome!
    </SectionTitle>
  )
}

export default Home
