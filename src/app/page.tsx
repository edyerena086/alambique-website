// Import core libraries
import React, { FC } from 'react'

// Import components
import { Section, SectionTitle } from '@app/components'

const Home: FC = () => {
  return (
    <Section>
      <SectionTitle>
        Welcome!
      </SectionTitle>
    </Section>
  )
}

export default Home
